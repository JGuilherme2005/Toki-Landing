{
  "name": "toki-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Public marketing website for the TOKI desktop focus application.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "html-validate index.html",
    "check:assets": "bun scripts/check-assets.mjs",
    "check:links": "bun scripts/check-links.mjs",
    "test": "bun run lint && bun run check:assets && bun run check:links && bun run build"
  },
  "devDependencies": {
    "html-validate": "^10.4.0",
    "vite": "^7.0.6"
  }
}
